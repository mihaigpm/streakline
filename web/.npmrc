# Always resolve packages from the public npm registry.
# This overrides any private/global registry (e.g. a corporate Artifactory)
# so the lockfile and CI builds (DigitalOcean App Platform) stay portable.
registry=https://registry.npmjs.org/
